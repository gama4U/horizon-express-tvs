import express, { Request, Response } from 'express';

const tourVoucherRouter = express.Router();

tourVoucherRouter.post('/', async (req: Request, res: Response) => {
});


export default tourVoucherRouter;
