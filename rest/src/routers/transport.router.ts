import express, { Request, Response } from 'express';

const transportVoucherRouter = express.Router();

transportVoucherRouter.post('/', async (req: Request, res: Response) => {
});


export default transportVoucherRouter;
